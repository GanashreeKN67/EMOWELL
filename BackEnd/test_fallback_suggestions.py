"""Deprecated test module for fallback suggestions."""


def main() -> None:
    print(
        "Fallback suggestion helpers have been removed. "
        "Gemini responses now drive all guidance, so this test is no longer applicable."
    )


if __name__ == "__main__":  # pragma: no cover
    main()